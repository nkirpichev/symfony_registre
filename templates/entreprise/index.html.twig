{% extends 'base.html.twig' %}

{% block title %}Liste des entreprises{% endblock %}

{% block body %}
{#<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style><div class="example-wrapper"> </div>#}


    <h1>Liste des entreprises</h1>

     <table class="table mx-3 fs-6">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for client in entreprises %}
            <tr>
                <td>{{ client.id }}</td>
                <td><a href="{{ path('app_entreprise_detail', {'id': client.id}) }}">{{ client.nom }}</a></td>
                <td>
                    
                    <a href="{{ path('app_entreprise_edit', {'id': client.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class='btn btn-primary' href="{{ path('app_entreprise_new') }}">Ajouter entreprise</a> 

{% endblock %}
