{% extends 'base.html.twig' %}

{% block title %}TacheStatut{% endblock %}

{% block body %}
    <h1>TacheStatut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tache_statut.id }}</td>
            </tr>
            <tr>
                <th>DateChangement</th>
                <td>{{ tache_statut.dateChangement ? tache_statut.dateChangement|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tache_statut_index') }}">back to list</a>

    <a href="{{ path('app_tache_statut_edit', {'id': tache_statut.id}) }}">edit</a>

    {{ include('tache_statut/_delete_form.html.twig') }}
{% endblock %}
