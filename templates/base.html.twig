<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <div class="container p-4">

    <div class="container">
        <header class="d-flex justify-content-center py-3">
        <ul class="nav nav-pills">
            <li class="nav-item"><a class="nav-link" href="{{ path('app_main') }}">Accueil</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('app_entreprise') }}">Entreprises</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('app_projet_index') }}">Projets</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('app_tache_index') }}">Taches</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('app_facture_index') }}">Factures</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('app_personne_index') }}">Personnes</a></li>  
            <li class="nav-item"><a class="nav-link" href="{{ path('app_categorie_index') }}">Categories</a></li>                         
        </ul>
        </header>
    </div>

       {# <a href="{{ path('app_main') }}">accueil</a>
        <a href="{{ path('app_entreprise') }}">entreprise</a>
        <a href="{{ path('app_projet_index') }}">projet</a>
        <a href="{{ path('app_tache_index') }}">tache</a>
        <a href="{{ path('app_facture_index') }}">facture</a>
        <a href="{{ path('app_personne_index') }}">personne</a>
        <a href="{{ path('app_categorie_index') }}">categorie</a>
        
        <a href="{{ path('app_statut_index') }}">statut</a>#}
        <style>
            .example-wrapper { margin: 1em auto; max-width: 1500px; width: 90%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
            label {padding: 5px }
        </style>
        <div class="example-wrapper">
            {% block body %}{% endblock %}
        </div>
        </div>
    </body>
</html>
