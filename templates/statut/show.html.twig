{% extends 'base.html.twig' %}

{% block title %}Statut{% endblock %}

{% block body %}
    <h1>Statut</h1>

    <table class="table mx-3">
        <tbody>
            <tr>
                <th class="w-25">Id</th>
                <td>{{ statut.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ statut.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_index') }}">back to list</a>

    <a href="{{ path('app_statut_edit', {'id': statut.id}) }}">modifier</a>

    {{ include('statut/_delete_form.html.twig') }}
{% endblock %}
